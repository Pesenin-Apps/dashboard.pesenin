export interface TableSectionTypes {
  _id: string;
  name: string;
  code: string;
}

export interface TableTypes {
  _id: string;
  name: string;
  number: number;
  used: boolean;
}

export interface signInTypes {
  email: string;
  password: string;
}
